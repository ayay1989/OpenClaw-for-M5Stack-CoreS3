# AGENTS.md

本文件是本仓库的 Agent 协作与工程治理规则，适用于整个项目。

## 基本原则

1. 默认采用 fail-fast 策略：发现编译、协议、硬件初始化、测试或文档状态不一致时，先停止扩展功能，优先修正问题。
2. 用户是 0 代码基础，所有进展、风险和结论必须用中文说明，并尽量用非技术语言解释影响。
3. 不要把聊天当成唯一进度来源。关键结论、计划、验收状态和已知问题必须落到仓库文档。
4. 不允许为了“看起来完成”而隐藏失败。硬件不可验证、测试无法运行、功能只是占位时必须明确标注。

## 权威进度视图

1. 项目中只能维护一个权威进度视图，不允许创建多个相互独立的进度来源。
2. 修改任何计划或进度文档时，必须优先更新原任务状态，其次记录变更日志。
3. 不允许只追加日志而不更新任务状态。
4. 如果发现计划文档与实际实现状态不一致，应优先修正文档，再继续开发。

## Sub-agent 协作要求

本项目鼓励活用 sub-agent。只要任务涉及多模块修改、硬件驱动、协议、Windows bridge、README/开源治理或回归测试，就应拆分角色协作。

推荐角色：

1. 规划 Agent：负责拆分工程、维护任务状态、判断优先级和收敛范围。
2. Coding Agent：负责具体代码实现，遵守现有模块边界，不做无关重构。
3. Code Reviewer：负责代码审查，优先找 bug、重复逻辑、长期维护风险和测试缺口。
4. Debug Agent：负责定位编译失败、运行异常、硬件日志、TCP/bridge 连接和初始化时序问题。
5. 漂移审查 Agent：负责对照用户原始需求、协议文档、硬件验收日志，确认实现没有偏离目标。
6. QA 回归 Agent：负责运行可用的本地测试、无硬件测试、静态检查，并整理未能验证的真机项目。
7. 代码治理 Agent：负责识别重复模块、过胖文件、职责混杂、协议兼容风险和后续扩展边界。

主 Agent 必须负责监工和最终整合，不能把子任务结论原样堆给用户。最终同步应给出：做了什么、验证了什么、还缺什么、下一步怎么做。

## 代码审计与 Review

1. 任何较大功能合并前，应安排 Code Reviewer 或审计 Agent 检查。
2. Review 输出必须优先列风险和问题，再列总结。
3. 审计重点包括：
   - 是否出现重复实现同一能力的模块。
   - 协议层是否混入过多业务逻辑。
   - Windows bridge 是否和固件端重复做同一套自动反应。
   - 硬件能力上报是否真实，不能硬编码成功。
   - 是否还能支持后续接入身体输入、人脸追踪、语音和 OpenClaw 记忆。
4. 对用户反馈的真机现象，优先相信现场日志和现象，再回查代码。

## 固件开发规则

1. 固件必须使用 ESP-IDF v5.x，纯 C，不使用 Arduino 框架，不引入 C++。
2. CoreS3 相关初始化顺序、PY32 电源、I2C 频率、LED、servo、touch 等硬件细节必须谨慎处理。
3. 硬件能力必须区分“命令支持”和“物理设备可用”。例如 LED、servo、audio 不允许简单写死为可用。
4. 驱动层应保持清晰边界：
   - `py32driver.*` 负责 PY32/I2C 扩展控制。
   - `leddriver.*` 负责 LED 写入和效果。
   - `servodriver.*` / `scservo_bus.*` 负责舵机和总线。
   - `body_service.*` 负责身体动作队列。
   - `protocol.*` 不应无限膨胀为所有业务逻辑的总入口。
5. 新增身体输入时，应优先归一到 `body_input` 语义，再兼容旧事件。

## Windows Bridge 规则

1. Windows bridge 是 OpenClaw 和 StackChan 的连接层，不能只做演示脚本，要按长期运行服务设计。
2. Bridge 自动反应策略应集中维护，避免同时对 `pressure`、`body_input`、`button`、`gesture` 重复触发同一动作。
3. 日志必须对 0 代码基础用户友好，能看出：是否监听成功、设备是否接入、hello 是否收到、TTS/音频/动作是否可用。
4. 安装脚本和启动脚本应尽量做到 Windows 即开即用，并明确失败原因。

## 文档与开源治理

1. README 面向开源用户，默认中文优先，清楚说明当前能力、安装方式、协议、硬件限制和验证方法。
2. 不要把未完成能力写成已完成能力。
3. 对未来计划要区分：
   - 已实现。
   - 已有代码但需真机验证。
   - 已规划但尚未实现。
4. GPL-3.0 许可证相关描述必须保持一致。

## 前后端边界

1. 前端工程默认不得修改后端脚本。
2. 如果必须修改后端脚本，必须单独开 Backend Compatibility PR。
3. 本仓库当前重点是固件、Windows bridge、协议和硬件治理；不要引入无关前端工作。

## 提交前检查

提交或推送前至少检查：

1. 工作区没有意外文件。
2. 相关文档状态已更新。
3. 能跑的测试已经运行。
4. 不能跑的测试和原因已说明。
5. 没有提交 WiFi 密码、API key、token、个人记忆内容等敏感信息。
